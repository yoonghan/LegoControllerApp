import React from 'react';
import {
  StyleSheet,
  View,
  Text
} from 'react-native';

const Loading: () => React$Node = (props) => {
  return (
    <Text>Loading...</Text>
  )
}

export default Loading;
